/*
import sc from "../../common/styles/counter.module.scss";

export const CounterValue = () => {
  return (
    <div className={`${sc.counter__value} ${counter === maxValue && 'error'}`}>
      {
        hasError
          ? (<span>Incorrect Value!</span>)
          : btnSetActive
            ? counter
            : 'Enter values and press "Set" button'
      }
    </div>
  )
}*/
